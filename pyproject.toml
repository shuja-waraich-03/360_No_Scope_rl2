[project]
name = "360-no-scope-rl2"
version = "0.1.0"
description = "A 2D medieval platformer where an AI agent learns to execute the perfect mid-air 360 no-scope shot using deep reinforcement learning (PPO via Stable Baselines 3 + Godot RL Agents)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "godot-rl>=0.8.2",
    "onnxscript>=0.6.2",
]
